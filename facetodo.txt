(A) Initialize Go module `mood-music` with Bubble Tea TUI framework and Cobra CLI, define root command with `--config`, `--log-level`, `--sampling-interval` global flags +cli
(A) Set up Python package `mood-engine` with pyproject.toml containing deps: opencv-python-headless, deepface, numpy, Pillow +pipeline
(A) Define IPC protocol between Go and Python: Go spawns Python `mood-engine` as long-running child process, bidirectional JSON-over-stdin/stdout with message types: `capture_request`, `emotion_result`, `health_check`, `shutdown` +ipc
(A) Implement Go-side IPC manager: spawn Python subprocess on app start, send capture requests at configured interval, read emotion JSON responses, restart subprocess on crash with exponential backoff (max 3 retries) +ipc
(A) Implement Python-side IPC event loop: read JSON from stdin, dispatch to webcam capture + emotion detection pipeline, write JSON results to stdout, handle SIGTERM gracefully +ipc
(A) Create config file schema at `~/.config/mood-music/config.toml` with sections: `[capture]` (interval_seconds, camera_index, resolution), `[emotion]` (model_backend, confidence_threshold, smoothing_window), `[spotify]` (client_id, client_secret, redirect_uri), `[mapping]` (mood presets), `[tui]` (theme, layout) +cli
(A) Implement `mood-music configure` subcommand: interactive Bubble Tea form to set camera index, sampling interval, Spotify credentials, and mood-music mapping preferences, write to config.toml +cli
(A) Implement webcam capture module in Python using OpenCV: open camera by index from config, capture single frame as numpy array, release camera between captures to minimize resource hold, return base64-encoded JPEG +capture
(A) Implement macOS camera access using AVFoundation via OpenCV backend (`cv2.CAP_AVFOUNDATION`), handle permission prompt gracefully by detecting authorization status and surfacing user-friendly error if denied +capture
(A) Implement Linux camera access using V4L2 via OpenCV backend (`cv2.CAP_V4L2`), auto-detect `/dev/video*` devices if camera_index not configured +capture
(A) Add camera health check: on startup, attempt to open and read one frame, return camera resolution and FPS capability in health_check response, fail fast with descriptive error if no camera found +capture
(A) Implement face detection preprocessing in Python using OpenCV Haar cascades or DeepFace built-in detector: crop frame to face region, reject frames with no face detected (return `no_face` status) +emotion
(A) Implement local emotion detection using DeepFace with MobileNet backend: accept cropped face image, run `DeepFace.analyze(actions=['emotion'])`, return emotion dict with probabilities for angry, disgust, fear, happy, sad, surprise, neutral +emotion
(A) Implement emotion model abstraction: factory accepting backend string ("deepface_mobilenet" | "deepface_opencv" | "cloud_aws" | "cloud_google"), returning unified interface `detect(frame_bytes) -> EmotionResult` with emotion_label, confidence, all_scores dict +emotion
(A) Implement sliding window emotion smoother in Python: maintain circular buffer of last N emotion results (default N=5, configurable via `smoothing_window`), compute weighted moving average of emotion scores (recent frames weighted higher), return dominant smoothed emotion +emotion
(A) Define mood taxonomy mapping emotions to mood states: FOCUS (neutral high + happy moderate), STRESSED (angry + fear + disgust above threshold), RELAXED (happy high + neutral), TIRED (sad high + neutral), ENERGIZED (surprise + happy high) — configurable thresholds in config.toml +mapping
(A) Implement mood classifier in Python: accept smoothed emotion scores, apply taxonomy thresholds from config, return current MoodState enum with confidence score +mapping
(A) Implement mood transition debouncing: require new mood to persist for minimum 3 consecutive windows before triggering playlist change, prevent rapid oscillation between moods +mapping
(A) Implement Spotify OAuth2 PKCE flow in Go: open browser to Spotify authorize URL with scopes (user-read-playback-state, user-modify-playback-state, user-read-currently-playing, playlist-read-private), listen on localhost callback, exchange code for access+refresh tokens +spotify
(A) Store Spotify tokens in `~/.config/mood-music/tokens.json` with access_token, refresh_token, expires_at fields, encrypted at rest using OS keychain via `go-keyring` library +spotify
(A) Implement Spotify token auto-refresh in Go: check expiry before each API call, refresh using refresh_token if expired, update stored tokens +spotify
(A) Implement Spotify playback state query: GET `/v1/me/player` to check if user has active device, current track, playback state (playing/paused), return structured SpotifyState +spotify
(A) Implement Spotify device selector: GET `/v1/me/player/devices` to list available devices, store preferred device_id in config, fall back to first active device +spotify
(A) Implement Spotify queue control: POST `/v1/me/player/queue` to add tracks to user's playback queue by Spotify URI +spotify
(A) Implement Spotify playback control: PUT `/v1/me/player/play` to start playback, PUT `/v1/me/player/pause` to pause, POST `/v1/me/player/next` to skip +spotify
(A) Implement Spotify search by audio features: GET `/v1/recommendations` with seed_genres and target audio features (energy, valence, tempo, instrumentalness) to get mood-appropriate track recommendations +spotify
(A) Define default mood-to-audio-features mapping in config: FOCUS → {energy:0.5-0.7, valence:0.4-0.6, tempo:100-130, instrumentalness:>0.5}, STRESSED → {energy:0.2-0.4, valence:0.5-0.8, tempo:60-90, instrumentalness:>0.7}, RELAXED → {energy:0.3-0.5, valence:0.6-0.9, tempo:70-100}, TIRED → {energy:0.6-0.8, valence:0.5-0.7, tempo:110-140}, ENERGIZED → {energy:0.7-0.9, valence:0.7-1.0, tempo:120-160} +mapping
(A) Allow user override of mood-to-audio-features mapping in config.toml under `[mapping.focus]`, `[mapping.stressed]` etc. with min/max ranges for each Spotify audio feature +mapping
(A) Implement mood-to-music bridge in Go: on mood transition event, query Spotify recommendations API with mapped audio features, queue top 5 recommended tracks, skip current track if mood change is significant (>2 mood levels apart) +spotify
(A) Add seed genre configuration per mood: default FOCUS→["electronic","ambient","classical"], STRESSED→["ambient","new-age","piano"], configurable in config.toml +mapping
(A) Implement Bubble Tea main model with app states: STARTUP (camera+Spotify check), DASHBOARD (main view), SETTINGS, DEVICE_SELECT, AUTH_FLOW +tui
(A) Build TUI dashboard layout with 5 zones: top bar (app title + current mood badge + clock), left column (ASCII webcam preview + emotion bars), center column (now playing + track info + playback controls), right column (mood history timeline), bottom bar (status messages + keybindings) +tui
(A) Implement ASCII webcam preview widget: receive base64 JPEG from Python, decode to image, downscale to fit widget dimensions, convert to ASCII art using luminance-to-character mapping (` .:-=+*#%@`), render in TUI with 4-frame-per-second refresh +tui
(A) Implement emotion probability bar chart widget: horizontal bars for each emotion (angry, happy, sad, neutral, surprise, fear, disgust), width proportional to probability, color-coded (happy=green, angry=red, sad=blue, neutral=gray, fear=yellow, surprise=cyan) +tui
(A) Implement current mood badge widget: large colored label showing current mood state (FOCUS/STRESSED/RELAXED/TIRED/ENERGIZED) with emoji-free icon using unicode block elements, pulsing border animation on mood transition +tui
(A) Implement "Now Playing" widget: display current Spotify track name, artist, album, with elapsed/total time progress bar, album art as small ASCII art if available +tui
(A) Implement playback control keybindings in TUI: Space=play/pause, n=next track, p=previous, +/- for volume (PUT `/v1/me/player/volume`), rendered as interactive footer controls +tui
(A) Implement mood history timeline widget: scrollable vertical list showing last 30 mood transitions with timestamps, mood label, confidence, and trigger emotion, newest at top +tui
(A) Implement mood history sparkline: horizontal sparkline chart showing mood confidence over last 60 sampling intervals, rendered with unicode braille characters (⣿⡇⠃) +tui
(A) Implement Tab key to cycle focus between webcam preview, now playing, mood history, and playback controls zones +tui
(A) Add real-time status bar: show camera status (active/inactive), Spotify connection status (connected/disconnected/refreshing), current sampling interval, Python subprocess health +tui
(A) Implement async event architecture in Go: use channels for emotion results (from IPC), Spotify state updates (from polling), user input (from Bubble Tea), tick events (sampling timer) — non-blocking TUI updates +tui
(A) Wire end-to-end pipeline: startup → camera check → Spotify auth → begin sampling loop → emotion detection → mood classification → mood transition → Spotify queue update → TUI refresh +cli
(B) Implement cloud emotion API fallback — AWS Rekognition: accept frame bytes, call `DetectFaces` with `Attributes=ALL`, parse emotion scores from response, normalize to same EmotionResult schema +emotion
(B) Implement cloud emotion API fallback — Google Cloud Vision: call `annotate` with `FACE_DETECTION` feature, parse emotion likelihoods (VERY_UNLIKELY to VERY_LIKELY), map to 0.0-1.0 scores +emotion
(B) Add `[emotion].backend` config option: "local" (default), "aws", "google" — factory selects implementation, cloud backends require credentials in config or env vars +emotion
(B) Implement adaptive sampling interval: if 5 consecutive frames return same dominant emotion with >0.8 confidence, increase interval by 2x (max 120s); if emotion shifts, reset to base interval — reduces CPU load during stable moods +capture
(B) Add camera resolution configuration: `[capture].resolution` accepting "low" (320x240), "medium" (640x480, default), "high" (1280x720) — lower = faster inference +capture
(B) Implement Spotify liked songs integration: GET `/v1/me/tracks` to fetch user's saved tracks, filter by audio features matching current mood, prefer familiar tracks over recommendations +spotify
(B) Add user playlist seeding: `[spotify].seed_playlists` config accepting list of Spotify playlist URIs, extract track pool from playlists, use as recommendation seeds alongside genre seeds +spotify
(B) Implement track blacklist: keybinding `b` on now-playing adds current track to `~/.config/mood-music/blacklist.json`, blacklisted tracks excluded from future queuing +spotify
(B) Implement track rating: keybinding `l` to "like" current track (save to Spotify), `d` to "dislike" (blacklist + skip), feed into recommendation refinement +spotify
(B) Add genre preference learning: track which recommended genres user skips vs listens through per mood, adjust genre seed weights over time, persist in `~/.local/share/mood-music/preferences.json` +spotify
(B) Implement session statistics overlay: `s` key shows session summary — total time, mood distribution pie chart (ASCII), tracks played count, average mood stability score +tui
(B) Add daily/weekly mood trend view: `t` key shows aggregated mood data from past sessions stored in `~/.local/share/mood-music/history.db` (SQLite), render as line chart with date x-axis +tui
(B) Implement face detection confidence display: show face detection bounding box overlaid on ASCII preview, display detection confidence percentage below preview +tui
(B) Add multi-face handling: if multiple faces detected, use largest face (closest to camera), display warning in status bar "Multiple faces detected — using primary" +emotion
(B) Implement ambient lighting compensation: detect mean frame luminance, warn user in status bar if too dark (<30) or too bright (>225), adjust image preprocessing (histogram equalization) before emotion detection +capture
(B) Add "do not disturb" mode: keybinding `D` pauses emotion detection and queue changes, maintains current playback, display DND badge in top bar, resume with `D` again +tui
(B) Implement manual mood override: keybinding `1-5` to manually set mood (1=FOCUS, 2=STRESSED, 3=RELAXED, 4=TIRED, 5=ENERGIZED), locks mood for configurable duration (default 10 min), shown as "manual" in mood history +tui
(B) Add smooth track transitions: when mood changes, let current track finish (or play to next natural break >75% through) before starting mood-appropriate queue, configurable via `[spotify].transition_mode` ("immediate" | "finish_track") +spotify
(B) Implement `mood-music auth` subcommand: standalone Spotify OAuth flow for initial setup without launching full TUI +cli
(B) Add `mood-music status` subcommand: headless check of camera availability, Spotify token validity, Python subprocess health — useful for troubleshooting +cli
(B) Implement `mood-music history` subcommand: query SQLite history DB, output mood session summaries as formatted table or JSON to stdout +cli
(B) Add `?` keybinding for help overlay listing all keybindings grouped by zone (global, playback, mood, navigation) +tui
(B) Implement color theme support: `[tui].theme` in config with "dark" (default), "light", "nord", "catppuccin" palettes, apply to all widgets consistently +tui
(B) Add Spotify currently-playing sync: poll `/v1/me/player/currently-playing` every 5s to sync TUI display if user changes track externally (phone, desktop app) +spotify
(B) Implement notification when Spotify Premium is required: detect 403 responses on playback control endpoints, display clear message that Premium is needed for playback control features +spotify
(B) Add `--dry-run` mode: run full pipeline (camera + emotion detection) but only log what playlist changes would be made without touching Spotify playback — useful for testing mood mapping +cli
(C) Implement local music file playback fallback: if Spotify unavailable, scan configured `[local].music_dir`, play files via `mpv` subprocess with JSON IPC for playback control +local
(C) Add local file mood tagging: analyze local audio files using `librosa` (Python) to extract tempo, energy, valence proxies, auto-tag files with mood categories, cache tags in SQLite +local
(C) Implement focus session timer: keybinding `F` starts Pomodoro-style timer (25min focus / 5min break), auto-set mood to FOCUS during work intervals and RELAXED during breaks, override emotion detection +tui
(C) Add productivity correlation view: if user runs focus sessions, show correlation between detected mood stability and session completion rate as scatter plot in stats overlay +tui
(C) Implement Spotify playlist export: keybinding `E` creates new Spotify playlist from all tracks played during current session, named "Mood Music — {date}" +spotify
(C) Add webcam privacy indicator: when camera is active, display green dot unicode character in top bar; when paused/DND, show red dot — visual confirmation of camera state +tui
(C) Implement emotion detection model hot-swap: `mood-music model set <backend>` switches emotion backend without restart, Python subprocess reloads model on next capture request +emotion
(C) Add screen brightness context: on macOS, read ambient light sensor via `ioreg`, factor into stress detection (low light + negative emotion = higher stress confidence) +capture
(C) Implement background mode: `mood-music --background` runs without TUI, only emotion detection + Spotify control, logs to file, controllable via `mood-music ctl pause|resume|status` Unix socket IPC +cli
(C) Add startup animation: brief ASCII art logo with fade-in effect on TUI launch while camera and Spotify initialize in background +tui
